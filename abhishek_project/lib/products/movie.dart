import 'package:abhishek_project/models/topRated_model.dart';
import 'package:flutter/material.dart';

class movieDes extends StatelessWidget {
  const movieDes({Key? key, required topRated currentPlace}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('currentPlace'),
      
    );
  }
}